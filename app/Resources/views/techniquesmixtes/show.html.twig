{% extends 'base.html.twig' %}

{% block body %}
    <h1>Techniquesmixte</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ techniquesMixte.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ techniquesMixte.nom }}</td>
            </tr>
            <tr>
                <th>Détails</th>
                <td>{{ techniquesMixte.détails }}</td>
            </tr>
            <tr>
                <th>Dimensions</th>
                <td>{{ techniquesMixte.dimensions }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if techniquesMixte.date %}{{ techniquesMixte.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('techniquesmixtes_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('techniquesmixtes_edit', { 'id': techniquesMixte.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
