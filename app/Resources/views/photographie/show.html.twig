{% extends 'base.html.twig' %}

{% block body %}
    <h1>Photographie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photographie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ photographie.nom }}</td>
            </tr>
            <tr>
                <th>Détails</th>
                <td>{{ photographie.détails }}</td>
            </tr>
            <tr>
                <th>Dimensions</th>
                <td>{{ photographie.dimensions }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if photographie.date %}{{ photographie.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('photographie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('photographie_edit', { 'id': photographie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
